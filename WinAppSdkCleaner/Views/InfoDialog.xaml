<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="WinAppSdkCleaner.Views.InfoDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinAppSdkCleaner.Views">

    <!-- left, top, right, bottom -->
    
    <ScrollView  ContentOrientation="Horizontal">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="16,8,0,5">
                <Image x:Name="LogoImage" Height="32" Margin="0,0,3,0"/>
                <TextBlock Name="HeadingText" FontSize="18" VerticalAlignment="Bottom"/>
            </StackPanel>

            <RichTextBlock x:Name="InfoText" Grid.Row="1" Margin="16,0,0,16" IsTextSelectionEnabled="True" TextWrapping="NoWrap"/>
        </Grid>
    </ScrollView>
</ContentDialog>
    