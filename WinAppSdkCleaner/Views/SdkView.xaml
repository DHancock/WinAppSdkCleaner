<Page 
    x:Class="WinAppSdkCleaner.Views.SdkView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinAppSdkCleaner.Views"
    xmlns:vm="using:WinAppSdkCleaner.ViewModels"
    NavigationCacheMode="Required">

    <!-- left, top, right, bottom -->
    
    <Grid Margin="15,3,15,9">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition MinWidth="110" Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style x:Key="TreeViewBorderStyle" TargetType="Border">
                <Setter Property="Padding" Value="4,0,0,0"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefault}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
        </Grid.Resources>

        <Border Style="{StaticResource TreeViewBorderStyle}">
            <TreeView x:Name="SdkTreeView" CanReorderItems="False" CanDrag="False" CanDragItems="False" SelectionChanged="SelectedTreeViewItemChanged" ItemInvoked="SdkTreeView_ItemInvoked" KeyDown="SdkTreeView_KeyDown">

                <TreeView.Resources>
                    <XamlUICommand x:Name="CopyCommand" Label="Copy" CanExecuteRequested="CopyCommand_CanExecuteRequested" ExecuteRequested="CopyCommand_ExecuteRequested">
                        <XamlUICommand.KeyboardAccelerators>
                            <!-- disabled because it would only be active after it's context menu has been opened, so handle it in code-->
                            <KeyboardAccelerator Key="C" Modifiers="Control" IsEnabled="False"/>
                        </XamlUICommand.KeyboardAccelerators>
                        <XamlUICommand.IconSource>
                            <SymbolIconSource Symbol="Copy"/>
                        </XamlUICommand.IconSource>
                    </XamlUICommand>

                    <XamlUICommand x:Name="InfoCommand" Label="Info..." CanExecuteRequested="InfoCommand_CanExecuteRequested" ExecuteRequested="InfoCommand_ExecuteRequested">
                        <XamlUICommand.KeyboardAccelerators>
                            <!-- disabled because it would only be active after it's context menu has been opened, so handle it in code-->
                            <KeyboardAccelerator Key="I" Modifiers="Control" IsEnabled="False"/>
                        </XamlUICommand.KeyboardAccelerators>
                        <XamlUICommand.IconSource>
                            <FontIconSource Glyph="&#xE946;"/>
                        </XamlUICommand.IconSource>
                    </XamlUICommand>  
                </TreeView.Resources>

                <TreeView.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{StaticResource CopyCommand}"/>
                        <MenuFlyoutItem Command="{StaticResource InfoCommand}"/>
                    </MenuFlyout>
                </TreeView.ContextFlyout>
                
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="TreeViewNode">
                        <TreeViewItem AutomationProperties.Name="{x:Bind ((vm:ItemBase)Content).AutomationName}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Image Height="20" Source="{x:Bind ((vm:ItemBase)Content).Logo}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ((vm:ItemBase)Content).HeadingText}" FontWeight="{x:Bind ((vm:ItemBase)Content).HeadingFontWeight}" Padding="4,0,2,0"/>
                                <TextBlock Grid.Column="2" x:Name="OtherAppsCountTextBlock" Text="{x:Bind ((vm:ItemBase)Content).OtherAppsCountStr}" Foreground="Crimson" Margin="4,0,0,0"/>
                            </Grid>
                        </TreeViewItem>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>

        <ProgressBar Grid.Row="1" Height="4" Margin="1" HorizontalAlignment="Stretch" x:Name="BusyIndicator" IsIndeterminate="false" Background="Transparent" BorderThickness="0" CornerRadius="2"/>

        <StackPanel Orientation="Vertical" Grid.Column="1" VerticalAlignment="Top" Margin="0,0,0,0">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="25,0,5,15"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
                <Style TargetType="FontIcon">
                    <Setter Property="Margin" Value="0,0,8,0"/>
                </Style>
            </StackPanel.Resources>

            <Button x:Name="RefreshButton" Command="{x:Bind SearchCommand}" AutomationProperties.Name="Refresh">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE149;" FontSize="12" />
                    <TextBlock Text="Refresh" />
                </StackPanel>
            </Button>

            <Button x:Name="SortButton" Command="{x:Bind SortCommand}" AutomationProperties.Name="Sort">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE174;" FontSize="13"/>
                    <TextBlock Text="Sort" />
                </StackPanel>
            </Button>
            
            <Button x:Name="RemoveButton" Command="{x:Bind RemoveCommand}" AutomationProperties.Name="Remove">
                <StackPanel Orientation="Horizontal">
                    <FontIcon x:Name="RemoveIcon" Glyph="&#xE74D;" FontSize="13" Foreground="Green" />
                    <TextBlock Text="Remove" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>